name: strategy_forge_demo
description: Demo app for the Strategy Forge editor plugin.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
dependencies:
  flutter:
    sdk: flutter
  strategy_forge:
    path: packages/strategy_forge
  public_file_saver: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/rinks/
